import 'package:flutter/material.dart';

class PointsScreen extends StatelessWidget {
  static const routeName = '/points-screen';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('No Points Yet ....')),
    );
  }
}
